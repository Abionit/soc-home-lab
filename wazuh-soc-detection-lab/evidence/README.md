# Evidence Guide

This folder tracks execution evidence for the Wazuh SOC Detection Lab.

Current status:

- Local lab structure, detection content, analytics outputs, and reporting artifacts are complete.
- Docker Desktop was validated locally.
- Wazuh certificate generation completed successfully.
- Wazuh image download is still pending because the Docker Hub pull did not complete during the execution window.

See `execution_status.md` for the current run status and resume commands.

Recommended evidence to capture after the Wazuh stack is running:

- Wazuh dashboard login screen after deployment
- Wazuh manager, indexer, and dashboard containers running
- Custom rules copied into the Wazuh manager
- Generated lab events replayed through syslog
- Triggered alerts visible in Wazuh
- MITRE ATT&CK fields in alert details
- Alert analytics outputs generated by Python
- Optional dashboard showing SOC metrics

Keep screenshots focused on evidence, not private credentials or local secrets.
